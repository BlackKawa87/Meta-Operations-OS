/// <reference types="vite/client" />

// Single Operator Mode: the frontend doesn't talk to Supabase directly
// anymore (no auth, uploads go through /api/**), so there are no
// client-exposed env vars to declare here right now.
